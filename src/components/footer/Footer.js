import styles from "../../../styles/Home.module.css";

const Footer = () => (
    <footer className={styles.footer}>
        <p>&copy; An Events App project Built in NextJs</p>
    </footer>
);

export default Footer;
